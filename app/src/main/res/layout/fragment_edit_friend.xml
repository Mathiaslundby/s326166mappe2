<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".EditFriendFragment">

    <TextView
        android:id="@+id/tvEditFriend"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/editFriend"
        android:textSize="26sp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"/>

    <EditText
        android:id="@+id/edit_friend_name"
        android:layout_below="@id/tvEditFriend"
        android:drawableStart="@drawable/ic_person"
        style="@style/etStyle"
        android:hint="@string/name"/>

    <EditText
        android:id="@+id/edit_friend_number"
        android:drawableStart="@drawable/ic_phone"
        style="@style/etStyle"
        android:layout_below="@id/edit_friend_name"
        android:hint="@string/number"/>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true">

        <ImageView
            android:id="@+id/btn_edit_friend"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_check_circle"
            android:layout_margin="10dp"/>

        <ImageView
            android:id="@+id/btn_delete_friend"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/delete_button"
            android:layout_margin="10dp"
            android:layout_toEndOf="@id/btn_edit_friend"/>

    </LinearLayout>

</RelativeLayout>